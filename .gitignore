# Node and package manager artifacts
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build and packaging output
*.tgz


# OS/editor junk
.DS_Store
Thumbs.db
.vscode-test/

# Midjourney workspace — experimental, local-only, must NOT be merged to main
# The entire folder is intentionally excluded from version control on main.
# It lives only on feature/agentic-icon-workflow as a local working area.
.midjourney-workspace/
